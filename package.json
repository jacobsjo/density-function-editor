{
    "name": "df-editor",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "init": "lerna init",
        "compile": "lerna run compile --stream",
        "postinstall": "lerna bootstrap",
        "devApp": "cd packages/app/ && npm run start"
    },
    "devDependencies": {
        "lerna": "^3.22.1",
        "tslint": "^5.20.1",
        "typescript": "^3.7.2"
    }
}
